[build-system]
requires = ["robotpy"]
build-backend = "robotpy.build"

[tool.robotpy]
robotCommand = "python -m robot"
robotpy_version = "2026.2.1"
requires = ["robotpy-rev==2026.0.2"]

[project]
name = "v9-swerve-basic"
version = "1.0.0"
